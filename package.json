{
  "name": "aletheia",
  "version": "1.0.0",
  "description": "This is a web extension made to reveal online food drive nutriscore and carbone impact.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:chrome": "webpack --env browser=chrome",
    "build:firefox": "webpack --env browser=firefox",
    "build": "npm run build:chrome && npm run build:firefox",
    "watch:chrome": "webpack --config webpack.config.js --watch --env browser=chrome",
    "watch:firefox": "webpack --config webpack.config.js --watch --env browser=firefox"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/chrome": "^0.0.277",
    "copy-webpack-plugin": "^12.0.2",
    "ts-loader": "^9.5.1",
    "typescript": "^5.6.3",
    "webpack": "^5.95.0",
    "webpack-cli": "^5.1.4"
  }
}